<h1>hiiiiii2</h1>
      <link rel="stylesheet" href="/css's/produtos.css">
<table>
    <div class="produto">
    {{#each produtos}}
    <div class="produto-item">
         <td>
          <img src="/img/{{imagem}}" width="40" height="40" alt="{{nome_produto}}">
          </td>

        <h2>{{nome_produto}}</h2>
        <p><strong>Descrição:</strong> {{descricao}}</p>
        <p><strong>Preço:</strong> {{preco}} Kz</p>
    </div>
    {{/each}}
</div>
